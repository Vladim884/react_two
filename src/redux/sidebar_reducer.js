let initialState = {
    friends: [
        { id: 11, name: "Kolya" },
        { id: 12, name: "Olya" },
        { id: 13, name: "Tolya" },
      ],
}
const sidebarReducer = (state = initialState, action) => {
    
    return state;
}

export  default sidebarReducer;
